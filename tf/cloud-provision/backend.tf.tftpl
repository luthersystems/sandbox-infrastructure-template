# AUTOGENERATED: DO NOT MODIFY

terraform {
  backend "s3" {
    bucket               = "${bucket}"
    region               = "${region}"
    workspace_key_prefix = "${workspace_key_prefix}"
    key                  = "${key}"
    kms_key_id           = "${kms_key_id}"
    role_arn             = "${role_arn}"
    encrypt              = true
  }
}
